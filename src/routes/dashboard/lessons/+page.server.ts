import type { PageServerLoad } from "./$types";

export const load: PageServerLoad = async () => {
    //const lessons = await getUserLessons(userId);
    //get from lessons the baseLesson only and return


};