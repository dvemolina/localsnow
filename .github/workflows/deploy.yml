name: Build and Deploy LocalSnow

on:
  push:
    branches:
      - "main"

permissions:
  packages: write
  contents: read

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          # The image is published under Dave's GHCR namespace, not the repo owner.
          # Use the existing package PAT instead of the repo-scoped GITHUB_TOKEN,
          # which cannot push this package reliably after the repo/org move.
          username: dvemolina
          password: ${{ secrets.GIT_PAT }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/dvemolina/localsnow:latest
            ghcr.io/dvemolina/localsnow:${{ github.sha }}

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push-image
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: dvemolina
          password: ${{ secrets.GIT_PAT }}

      - name: Create environment file
        run: |
          echo "GIT_COMMIT_HASH=${{ github.sha }}" >> envfile

      - name: 'Stack Deploy'
        uses: cssnr/stack-deploy-action@v1
        with:
          name: 'localsnow'
          file: 'docker-compose.production.yml'
          host: ${{ secrets.DEPLOY_HOST }}
          port: ${{ secrets.DEPLOY_PORT }}
          user: ${{ secrets.DEPLOY_USER }}
          ssh_key: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
          env_file: ./envfile
          registry_host: 'ghcr.io'
          registry_user: dvemolina
          registry_pass: ${{ secrets.GIT_PAT }}
          summary: true

  cleanup:
    name: 'Cleanup'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: deploy
    permissions:
      contents: read
      packages: write

    steps:
      - name: 'Purge Cache'
        uses: cssnr/cloudflare-purge-cache-action@v2
        with:
          token: ${{ secrets.CLOUDFLARE_ZONE_PURGE_TOKEN }}
          zones: localsnow.org
